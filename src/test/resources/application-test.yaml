spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/movies_test_db
    username: postgres
    password: 1111
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: dev.profitsoft.internship.rebrov.blocktwo.config.CustomPostgreSQLDialect
        format_sql: true
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db_changelog.xml
    drop-first: true